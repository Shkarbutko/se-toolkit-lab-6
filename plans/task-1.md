# Task 1 Plan

## Goal

Create a simple CLI agent that can call an LLM using environment variables.

## Steps

1. Create `agent.py`.
2. Read LLM configuration from environment variables.
3. Send a user question to the LLM.
4. Return the response as JSON.
5. Add regression tests.
6. Document the architecture in `AGENT.md`.
